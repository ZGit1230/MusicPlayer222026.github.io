/* DIVs 2D Rectangles
*/
//
println(displayWidth, displayHeight);
fullScreen();
int appWidth = displayWidth;
int appHeight = displayHeight;
//
int paperWidth = 800;
int paperHeight = 1300;


//rect(DivX, DivY, DivWidth, DivHeight);
float menuDivX = appWidth * 50 / paperWidth;
float menuDivY = appHeight * 50 / paperWidth;
float menuDivWidth = appWidth * 100 / paperWidth;
float menuDivHeight = appHeight * 100 / paperWidth;
rect(menuDivX, menuDivY, menuDivWidth, menuDivHeight);

float optionsDivX = appWidth * 650 / paperWidth;
float optionsDivY = appHeight * 50 / paperWidth;
float optionsDivWidth = appWidth * 100 / paperWidth;
float optionsDivHeight = appHeight * 100 / paperWidth;
rect(optionsDivX, optionsDivY, optionsDivWidth, optionsDivHeight);

float exitDivX = appWidth * 550 / paperWidth;
float exitDivY = appHeight * 50 / paperWidth;
float exitDivWidth = appWidth * 100 / paperWidth;
float exitDivHeight = appHeight * 100 / paperWidth;
rect(exitDivX, exitDivY, exitDivWidth, exitDivHeight);

float authorDivX = appWidth * 250 / paperWidth;
float authorDivY = appHeight * 300 / paperWidth;
float authorDivWidth = appWidth * 300 / paperWidth;
float authorDivHeight = appHeight * 50 / paperWidth;
rect(authorDivX, authorDivY, authorDivWidth, authorDivHeight);

float nameDivX = appWidth * 220 / paperWidth;
float nameDivY = appHeight * 350 / paperWidth;
float nameDivWidth = appWidth * 240 / paperWidth;
float nameDivHeight = appHeight * 70 / paperWidth;
rect(nameDivX, nameDivY, nameDivWidth, nameDivHeight);

float logoDivX = appWidth * 200 / paperWidth;
float logoDivY = appHeight * 420 / paperWidth;
float logoDivWidth = appWidth * 400 / paperWidth;
float logoDivHeight = appHeight * 400 / paperWidth;
rect(logoDivX, logoDivY, logoDivWidth, logoDivHeight);

float progBarDivX = appWidth * 250 / paperWidth;
float progBarDivY = appHeight * 880 / paperWidth;
float progBarDivWidth = appWidth * 300 / paperWidth;
float progBarDivHeight = appHeight * 50 / paperWidth;
rect(progBarDivX, progBarDivY, progBarDivWidth, progBarDivHeight);

float timerDivX = appWidth * 150 / paperWidth;
float timerDivY = appHeight * 880 / paperWidth;
float timerDivWidth = appWidth * 100 / paperWidth;
float timerDivHeight = appHeight * 50 / paperWidth;
rect(timerDivX, timerDivY, timerDivWidth, timerDivHeight);

float maxTimeDivX = appWidth * 550 / paperWidth;
float maxTimeDivY = appHeight * 880 / paperWidth;
float maxTimeDivWidth = appWidth * 100 / paperWidth;
float maxTimeDivHeight = appHeight * 50 / paperWidth;
rect(maxTimeDivX, maxTimeDivY, maxTimeDivWidth, maxTimeDivHeight);

float redoDivX = appWidth * 30 / paperWidth;
float redoDivY = appHeight * 850 / paperWidth;
float redoDivWidth = appWidth * 100 / paperWidth;
float redoDivHeight = appHeight * 100 / paperWidth;
rect(redoDivX, redoDivY, redoDivWidth, redoDivHeight);

float loopDivX = appWidth * 780 / paperWidth;
float loopDivY = appHeight * 850 / paperWidth;
float loopDivWidth = appWidth * 100 / paperWidth;
float loopDivHeight = appHeight * 100 / paperWidth;
rect(loopDivX, loopDivY, loopDivWidth, loopDivHeight);

float prevDivX = appWidth * 150 / paperWidth;
float prevDivY = appHeight * 1000 / paperWidth;
float prevDivWidth = appWidth * 100 / paperWidth;
float prevDivHeight = appHeight * 100 / paperWidth;
rect(prevDivX, prevDivY, prevDivWidth, prevDivHeight);

float rewindDivX = appWidth * 250 / paperWidth;
float rewindDivY = appHeight * 1000 / paperWidth;
float rewindDivWidth = appWidth * 100 / paperWidth;
float rewindDivHeight = appHeight * 100 / paperWidth;
rect(rewindDivX, rewindDivY, rewindDivWidth, rewindDivHeight);

float playPauseDivX = appWidth * 350 / paperWidth;
float playPauseDivY = appHeight * 1000 / paperWidth;
float playPauseDivWidth = appWidth * 100 / paperWidth;
float playPauseDivHeight = appHeight * 100 / paperWidth;
rect(playPauseDivX, playPauseDivY, playPauseDivWidth, playPauseDivHeight);

float fastFowardDivX = appWidth * 450 / paperWidth;
float fastFowardDivY = appHeight * 1000 / paperWidth;
float fastFowardDivWidth = appWidth * 100 / paperWidth;
float fastFowardDivHeight = appHeight * 100 / paperWidth;
rect(fastFowardDivX, fastFowardDivY, fastFowardDivWidth, fastFowardDivHeight);

float nextDivX = appWidth * 550 / paperWidth;
float nextDivY = appHeight * 1000 / paperWidth;
float nextDivWidth = appWidth * 100 / paperWidth;
float nextDivHeight = appHeight * 100 / paperWidth;
rect(nextDivX, nextDivY, nextDivWidth, nextDivHeight);
